name: app_final
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.2
  shared_preferences: ^2.0.13
  supabase_flutter: ^2.0.2
  flutter_dotenv: ^5.0.2
  bcrypt: ^1.1.3
  image_picker: ^1.0.5
  image_cropper: ^5.0.1
  flutter_image_compress: ^1.1.0
  location: ^5.0.3
  provider: ^6.0.0
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets: 
    - supabase_initialize.env
